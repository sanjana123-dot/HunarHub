@@ -1,28 +1,25 @@
# Server Configuration
# Server
server.port=${PORT:8080}

# Database Configuration
spring.datasource.url=jdbc:postgresql://aws-1-ap-northeast-1.pooler.supabase.com:5432/postgres?sslmode=require
spring.datasource.username=postgres.vcdmhspdidwjeqfwshan
spring.datasource.password=Aegontargar1#
spring.datasource.driver-class-name=org.postgresql.Driver
# Active Profile
spring.profiles.active=dev

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# JWT Configuration
jwt.secret=HunarHubSecretKeyForJWTTokenGeneration2024SecureAndLongEnough
jwt.expiration=86400000

# File Upload Configuration (max 5MB to match frontend validation)
# File Upload
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB

# CORS Configuration
spring.web.cors.allowed-origins=http://localhost:3000
# CORS
spring.web.cors.allowed-origins=${CORS_ORIGIN:http://localhost:3000}
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*

# JWT
jwt.secret=${JWT_SECRET}
jwt.expiration=86400000
